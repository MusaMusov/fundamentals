function solve (letter) {
    let isUpper = false;

    if(letter === letter.toUpperCase()) {
        isUpper = true;
        console.log("upper-case");
    } else {
        console.log("lower-case");
    }

}